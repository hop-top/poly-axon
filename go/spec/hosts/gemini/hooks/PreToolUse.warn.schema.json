{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "gemini PreToolUse warn",
  "type": "object",
  "required": ["decision", "reason", "systemMessage"],
  "additionalProperties": false,
  "properties": {
    "decision": { "type": "string", "enum": ["allow"] },
    "reason": { "type": "string" },
    "systemMessage": { "type": "string" }
  }
}
