{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "gemini PreToolUse block",
  "type": "object",
  "required": ["decision", "reason"],
  "additionalProperties": false,
  "properties": {
    "decision": { "type": "string", "enum": ["deny"] },
    "reason": { "type": "string" }
  }
}
